<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_dialog_date"
        tools:text="时间"
        android:gravity="center"
        android:padding="8dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingHorizontal="10dp"
        android:paddingBottom="5dp"
        tools:context="cool.ldr.pnet.views.dialog.DateDialog">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_date_year"
            android:layout_width="0px"
            android:layout_height="wrap_content"
            android:layout_weight="1.2"
            tools:itemCount="3"
            android:scrollbars="none"
            tools:listitem="@layout/picker_item" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_date_month"
            android:layout_width="0px"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:scrollbars="none"
            tools:itemCount="3"
            tools:listitem="@layout/picker_item" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_date_day"
            android:layout_width="0px"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:scrollbars="none"
            tools:itemCount="3"
            tools:listitem="@layout/picker_item" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_date_hour"
            android:layout_width="0px"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:scrollbars="none"
            tools:itemCount="3"
            tools:listitem="@layout/picker_item" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_date_min"
            android:layout_width="0px"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:scrollbars="none"
            tools:itemCount="3"
            tools:listitem="@layout/picker_item" />

    </LinearLayout>
</LinearLayout>